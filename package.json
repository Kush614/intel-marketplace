{
  "name": "intek",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "install:all": "cd worldmonitor && npm install",
    "dev": "cd worldmonitor && npm run dev",
    "build": "cd worldmonitor && npm install && npm run build",
    "preview": "cd worldmonitor && npm run preview"
  }
}
